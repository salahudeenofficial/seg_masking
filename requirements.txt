# Core dependencies - MUST install in this order
# Install PyTorch first (check https://pytorch.org for your CUDA version)
# For CUDA 11.8:
# torch>=2.0.0
# torchvision>=0.15.0
# For CPU only:
torch>=2.0.0
torchvision>=0.15.0

# NumPy and image processing
numpy>=1.21.0,<2.0.0
Pillow>=8.3.0
opencv-python>=4.5.0

# SegFormer dependencies
transformers>=4.20.0
timm>=0.6.0

# OpenPose dependencies
einops>=0.3.0

# Utilities
tqdm>=4.62.0
scipy>=1.7.0  # For label smoothing

# Note: mmcv-full is removed - not needed for SegFormer and causes build issues
# If you need mmcv, install separately after torch:
# pip install mmcv-full -f https://download.openmmlab.com/mmcv/dist/cu118/torch2.0/index.html

